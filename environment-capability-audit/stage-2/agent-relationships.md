# Area 2 — Agent Infrastructure & Relationship Evaluation
## Comprehensive Audit of the 15 Core Agent Interfacing Vectors

Each relationship has been evaluated against empirical execution in this Replit environment.

| Relationship Vector | Status Classification | How Discovered / Evidence | How to Activate | Persistence | What It Enables |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1. AGENT → SHELL** | **VERIFIED / AUTHENTICATED** | Executed bash 5.3 commands via `run_command`, `execSync`, subshell invocation. | Invoke tool `run_command` or Node `child_process`. | Subshell / background task | Arbitrary binary execution, script running, system inspection. |
| **2. AGENT → FILESYSTEM** | **VERIFIED / AUTHENTICATED** | Full read/write/edit access on `/home/runner/workspace`, `/tmp`, `/home/runner/.config`. | Direct node `fs` operations, tool calls (`write_to_file`, `replace_file_content`). | Persistent (Workspace) / Ephemeral (`/tmp`) | Workspace code modification, logging, artifact creation. |
| **3. AGENT → NETWORK** | **VERIFIED / UNAUTHENTICATED** | Outbound HTTP/HTTPS, WebSockets, DNS, TCP/UDP sockets confirmed functional. | Standard HTTP clients (`fetch`, `curl`, `axios`, `net.Socket`). | Connection lifetime | External API consumption, package downloading, web scraping. |
| **4. AGENT → DATABASE** | **VERIFIED / AUTHENTICATED** | Replit KV Store (`REPLIT_DB_URL`) & PostgreSQL (`helium:5432`) open and writable. | `curl -X POST $REPLIT_DB_URL` or `drizzle-orm` / `pg` connection to `helium`. | Persistent DB storage | Relational ORM data management & key-value state persistence. |
| **5. AGENT → BROWSER** | **VERIFIED / AUTHENTICATED** | Executed Chromium 140 (`REPLIT_PLAYWRIGHT_CHROMIUM_EXECUTABLE`) with CJK support. | `playwright-core` `chromium.launch({ executablePath })` or TigerVNC display. | Ephemeral browser session | Automated browser testing, DOM inspection, visual screenshotting. |
| **6. AGENT → GITHUB** | **VERIFIED / AUTHENTICATED** | Git 2.53, GitHub CLI `/repl/ctls/bin/gh` pre-installed & connected to Replit Connectors. | `/repl/ctls/bin/gh` or `git` CLI commands. | Session / Git credentials | Automated commit, branch, PR creation, and release management. |
| **7. AGENT → DEPLOYMENT** | **DOCUMENTED / POSSIBLE** | Replit deployment configuration in `.replit` (`deploymentTarget = "autoscale"`). | Configure `.replit` deployment target or trigger Replit deployment API. | Permanent production host | Publishing web apps to Autoscale or Reserved VM production domains. |
| **8. AGENT → BACKGROUND PROCESS** | **VERIFIED / AUTHENTICATED** | Process supervision via `pid1`/`pid2`, async tasks, background Express listeners. | Launch background commands, `nohup`, or `pnpm run dev`. | MicroVM session lifetime | Running server daemons, workers, and background monitoring tasks. |
| **9. AGENT → SCHEDULED JOB** | **DOCUMENTED / POSSIBLE** | Replit Scheduled Deployments feature & shell timer scheduling (`schedule`). | Deploy via Scheduled Deployment mode or run timer daemon. | Scheduled execution | Periodic batch processing, data aggregation, and recurring health audits. |
| **10. AGENT → EXTERNAL API** | **VERIFIED / UNAUTHENTICATED** | Outbound HTTPS requests to arbitrary public endpoints (NPM, PyPI, GitHub API). | `fetch('https://api.example.com')` or `axios`. | Request lifetime | Consuming third-party web services, webhooks, and AI provider APIs. |
| **11. AGENT → OTHER AGENT** | **VERIFIED / AUTHENTICATED** | `invoke_subagent`, `send_message`, Node IPC, and local HTTP REST agent endpoints. | Tool `invoke_subagent` or HTTP RPC server on local port. | Task / subagent lifecycle | Multi-agent collaboration, specialized task delegation, parallel audits. |
| **12. AGENT → MCP SERVER** | **VERIFIED / POSSIBLE** | `@modelcontextprotocol/sdk` installable; stdio JSON-RPC & HTTP transport supported. | Run MCP server process (`node mcp-server.js`) via stdio or SSE. | Process lifetime | Exposing custom agent tools over standardized Model Context Protocol. |
| **13. AGENT → AI MODEL** | **VERIFIED / AUTHENTICATED** | Native parent LLM Antigravity + `replit ai` Modelfarm CLI + OpenAI/Anthropic/Gemini SDKs. | Direct prompt execution or SDK calls (`new OpenAI()`). | Request lifetime | LLM reasoning, code analysis, image vision, and text generation. |
| **14. AGENT → CLOUD SERVICE** | **VERIFIED / POSSIBLE** | Google Cloud SDK (`gcloud`), AWS SDK installable, Replit GCS App Storage. | Execute `/repl/ctls/bin/gcloud` or import `@aws-sdk/client-s3`. | Cloud account lifetime | Provisioning cloud resources, uploading S3/GCS objects, running serverless. |
| **15. AGENT → PREVIEW / WEBVIEW** | **VERIFIED / AUTHENTICATED** | Replit Dev Domain (`REPLIT_DEV_DOMAIN`), Artifact Router daemon (`artifact-router`). | Serve HTTP application on port 5000/8080. | Active web app session | Real-time interactive UI previews, web app preview rendering. |
